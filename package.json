{
  "name": "superhostos",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:api": "pnpm --filter @superhostos/api dev",
    "dev:web": "pnpm --filter @superhostos/web dev",
    "build:api": "pnpm --filter @superhostos/api build",
    "build:web": "pnpm --filter @superhostos/web build"
  }
}
